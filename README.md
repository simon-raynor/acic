#Advanced Compoter Intelligenced Colors

First run `npm install` to download the required libraries

Then run either `npm run start` (to view the app locally) or `npm build`
(to build the app for production).
